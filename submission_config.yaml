blender:
  # Path to the Blender executable on the system
  exec_path: "/home/hershkr/dl_intro/DeepLearning_project3/blender-5.0.1-linux-x64/blender"
  # Path to the .blend file (relative to project root or absolute path)
  blend_file: "etl/blender/chess-set.blend"
  # Path to the python generation script (relative to project root or absolute path)
  script_file: "etl/blender/generate_synthetic_from_fen.py"

models:
  # Path to your trained model (relative, absolute, or just filename to be taken from huggingface). 
  bbdm_checkpoint: "bbdm_f16_mask_guided.pth"

evaluation:
  # Set to true to run evaluation automatically
  enabled: true

output:
  # Directory where results will be saved - NOTE: submission.py enforces ./results/ as per PDF requirements
  results_dir: "./results"
